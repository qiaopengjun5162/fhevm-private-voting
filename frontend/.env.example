# Frontend environment configuration
# Copy this file to .env.local and fill in your real values
# .env.local is gitignored by Next.js — never commit real credentials

# No sensitive keys required for the frontend.
# The relayer URL is already configured in src/lib/config.ts
